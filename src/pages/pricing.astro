---
import BaseLayout from '../layouts/BaseLayout.astro';
import PricingTable from '../components/services/PricingTable.astro';
import CTABanner from '../components/common/CTABanner.astro';
import { pricingData } from '../data/pricing';
import { seoData } from '../data/seo';

const seo = seoData.pricing;
---
<BaseLayout title={seo.title} description={seo.description}>
  <section class="py-16 md:py-24">
    <div class="max-w-content mx-auto px-4">
      <div class="text-center">
        <h1 class="text-section">서비스 요금 안내</h1>
        <p class="mt-4 text-lg text-neutral-800">
          유통스타트의 모든 서비스는 합리적인 가격으로 최고의 효율을 제공합니다.
        </p>
      </div>
      <div class="mt-16">
        <PricingTable serviceName={pricingData.utongstart.serviceName} packages={pricingData.utongstart.packages} />
        <PricingTable serviceName={pricingData.liveCommerce.serviceName} packages={pricingData.liveCommerce.packages} />
        <PricingTable serviceName={pricingData.placeSeo.serviceName} packages={pricingData.placeSeo.packages} />
      </div>
    </div>
  </section>
  <CTABanner />
</BaseLayout>
