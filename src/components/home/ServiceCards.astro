---
import { services } from '../../data/services';
---
<section class="py-16 md:py-24 bg-neutral-50">
  <div class="max-w-content mx-auto px-4">
    <div class="text-center">
      <h2 class="text-3xl md:text-5xl font-bold text-primary-950">사업 영역</h2>
      <p class="mt-4 text-lg text-neutral-800">우리는 제조사의 유통 고민을 함께 해결합니다.</p>
    </div>
    
    <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-8">
      {services.map(service => (
        <a href={service.href} class="bg-white p-8 rounded-2xl shadow-sm hover:shadow-lg transition-all border border-neutral-100 group">
          <div class="w-16 h-16 bg-primary-100 rounded-xl flex items-center justify-center group-hover:bg-primary-500 transition-colors overflow-hidden p-2">
            <img src={`/images/services/${service.icon}`} alt="" class="w-full h-full object-contain group-hover:brightness-0 group-hover:invert transition-all" />
          </div>
          <h3 class="mt-6 text-xl font-bold text-primary-950 group-hover:text-primary-500 transition-colors">{service.title}</h3>
          <p class="mt-4 text-neutral-800 leading-relaxed">{service.description}</p>
          <div class="mt-8 flex items-center text-primary-600 font-bold group-hover:translate-x-2 transition-transform">
            자세히 보기
            <svg class="w-5 h-5 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
