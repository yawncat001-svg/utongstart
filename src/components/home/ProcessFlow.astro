---
import { steps } from '../../data/steps';
---
<section class="py-16 md:py-24 bg-neutral-50 border-y border-neutral-100">
  <div class="max-w-content mx-auto px-4">
    <div class="text-center">
      <h2 class="text-3xl md:text-5xl font-bold text-primary-950">4단계 유통 프로세스</h2>
      <p class="mt-4 text-lg text-neutral-800">우리는 단계별로 제조사의 성장을 실질적으로 돕습니다.</p>
    </div>
    
    <div class="mt-12 grid grid-cols-1 md:grid-cols-4 gap-4">
      {steps.map((step, index) => (
        <div class="relative bg-white p-6 rounded-2xl shadow-sm hover:shadow-lg transition-all border border-neutral-100 ">
          <div class="flex items-center justify-between">
            <span class="text-accent-500 font-bold text-2xl">0{index + 1}</span>
            <span class="text-neutral-500 text-sm font-medium">{step.duration}</span>
          </div>
          <div class="mt-4 mb-2 h-24 w-full bg-primary-50 rounded-xl overflow-hidden flex items-center justify-center p-2">
            <img src={`/images/steps/${step.icon}`} alt={step.title} class="h-full object-contain" />
          </div>
          <h3 class="mt-4 text-xl font-bold text-primary-950 font-display leading-tight">{step.title}</h3>
          <p class="mt-2 text-primary-500 font-semibold text-sm">{step.subtitle}</p>
          <p class="mt-4 text-neutral-800 text-sm leading-relaxed">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
